const ColumnaTabla = ({
  titulo,
  campo,
  ancho = 1,
  alinear = 'left',
  render
}) => {
  return null // Este componente no renderiza nada
}

ColumnaTabla.displayName = 'ColumnaTabla'

export default ColumnaTabla
